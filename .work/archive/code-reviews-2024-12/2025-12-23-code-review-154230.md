# Code Review - 2025-12-23

## Status: APPROVED WITH SUGGESTIONS

## BLOCKING (Must Fix Before Merge)

None

## NON-BLOCKING (May Be Deferred)

**IF/ELSE conditionals documented but not implemented:**
- Description: The README.md documents IF/ELSE conditionals as workflow syntax, but the parser does not implement them. The documentation does include a warning that IF/ELSE is "NOT YET IMPLEMENTED", but the example workflow file (`code-review.workflow.md`) uses IF/ELSE syntax without the disclaimer.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/examples/code-review.workflow.md:20-23`
- Action: Either remove IF/ELSE from the example workflow, or add a comment in the example file noting this is aspirational syntax. The example should be a working reference, not demonstrate unimplemented features.

**Example workflow file references deleted:**
- Description: The README references `execute.workflow.md` but git status shows `plugin/hooks/examples/execute.workflow.md` is staged for deletion (AD status). This will leave the README referencing a non-existent file.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/README.md:1199-1200`
- Action: Either restore the deleted workflow files, update the README to remove references, or add new working examples.

**Race condition documented but not mitigated:**
- Description: The `trackTaskDispatch` function has a documented race condition for concurrent Task tool dispatches. While the comment correctly notes this is unlikely with current Claude Code behavior, a defensive guard would be cheap insurance.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/hooks/task-tracker.ts:17-19`
- Action: Consider adding a simple file-based mutex (using lockfile package) or atomic write pattern. Alternatively, leave as-is if the risk is truly negligible.

**Magic number for task ID generation:**
- Description: The task ID generation uses hardcoded slice values (`slice(2, 6)`, `slice(2, 8)`) without explaining the rationale.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/hooks/task-tracker.ts:7-9` and `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/state.ts:12-13`
- Action: Add a comment explaining the ID format and why these specific lengths were chosen (e.g., collision probability, readability).

**WorkflowStateManager.update spreads variables unconditionally:**
- Description: The update method uses `{ ...existing.variables, ...(updates.variables ?? {}) }` which means you cannot remove a variable once set. This is intentional per the fix commit, but the behavior should be documented.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/state.ts:83`
- Action: Add a comment explaining that variables can only be added/updated, never removed, and why this design decision was made.

**Emoji in context output:**
- Description: The `formatWorkflowContext` function uses an emoji (`⚠️`) for the BLOCKED warning. While visually helpful, this may not render correctly in all terminal environments.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/context.ts:62`
- Action: Consider using ASCII-safe markers like `[WARNING]` or `***` for better compatibility, or document that UTF-8 terminal support is expected.

**Console.error used instead of logger in workflow hooks:**
- Description: The `handleSubagentStop` and `trackTaskDispatch` functions use `console.error` for error logging, while the rest of the codebase uses the structured `logger` utility.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/hooks/subagent-stop.ts:78` and `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/hooks/task-tracker.ts:49`
- Action: Use `logger.error` or `logger.warn` for consistency with the rest of the codebase, enabling structured logging and log level filtering.

**Missing JSDoc on public API exports:**
- Description: The `src/workflow/index.ts` exports several public APIs but the source files lack comprehensive JSDoc documentation for the exported types and functions.
- Location: `/Users/tobyhede/psrc/turboshovel/plugin/hooks/hooks-app/src/workflow/index.ts`
- Action: Add JSDoc comments to `WorkflowState`, `Task`, `TaskState`, `WorkflowStateManager`, and `parseWorkflow` explaining their purpose and usage.

## Checklist

**Security & Correctness:**
- [x] No security vulnerabilities (SQL injection, XSS, CSRF, exposed secrets)
- [x] No insecure dependencies or deprecated cryptographic functions
- [x] No critical logic bugs (meets acceptance criteria)
- [x] No race conditions, deadlocks, or data races (documented race condition is low-risk)
- [x] No unhandled errors, rejected promises, or panics
- [x] No breaking API or schema changes without migration plan

**Testing:**
- [x] All tests passing (unit, integration, property-based where applicable)
- [x] New logic has corresponding tests (220 tests covering workflow system)
- [x] Tests cover edge cases and error conditions
- [x] Tests verify behavior (not implementation details)
- [ ] Property-based tests for mathematical/algorithmic code with invariants (not applicable)
- [x] Tests are isolated (independent, don't rely on other tests)
- [x] Test names are clear and use structured arrange-act-assert patterns

**Architecture:**
- [x] Single Responsibility Principle (functions/files have one clear purpose)
- [x] No non-trivial duplication (logic that if changed in one place would need changing elsewhere)
- [x] Clean separation of concerns (business logic separate from data marshalling)
- [x] No leaky abstractions (internal details not exposed)
- [x] No over-engineering (YAGNI - implement only current requirements)
- [x] No tight coupling (excessive dependencies between modules)
- [x] Proper encapsulation (internal details not exposed across boundaries)
- [x] Modules can be understood and tested in isolation

**Error Handling:**
- [x] No swallowed exceptions or silent failures
- [x] Error messages provide sufficient context for debugging
- [x] Fail-fast on invariants where appropriate

**Code Quality:**
- [x] Simple, not clever (straightforward solutions over complex ones)
- [x] Clear, descriptive naming (variables, functions, classes)
- [x] Type safety maintained (branded types for TaskNumber, discriminated unions for Action)
- [x] Follows language idioms and project patterns consistently
- [ ] No magic numbers or hardcoded strings (use named constants) - minor issue noted
- [x] Consistent approaches when similar functionality exists elsewhere
- [x] Comments explain "why" not "what" (code should be self-documenting)
- [ ] Rationale provided for non-obvious design decisions - some gaps noted
- [ ] Doc comments for public APIs - missing JSDoc on exports

**Process:**
- [x] No obvious performance issues (N+1 queries, inefficient algorithms on hot paths)
- [x] ALL linter warnings addressed by fixing root cause
- [x] Requirements met exactly (no scope creep)
- [x] No unnecessary reinvention (appropriate use of existing libraries/patterns)

## Next Steps

1. Address BLOCKING issues (if any) - None
2. Consider NON-BLOCKING suggestions - Prioritize fixing deleted example files
3. Ready to merge when status is APPROVED or APPROVED WITH SUGGESTIONS

---

## Review Context

**Branch:** `feat/workflow-system`
**Base:** 670ead1 (main)
**Head:** a5543dd
**Commits reviewed:** 36 commits

**Files changed:** 44 files
**Lines added:** ~6,971
**Lines removed:** ~14

**Key implementation areas:**
1. **File pattern filtering** (`src/dispatcher.ts`, `src/config.ts`) - minimatch-based filtering for monorepo support
2. **Workflow types** (`src/workflow/types.ts`) - Branded TaskNumber, discriminated Action union, WorkflowState
3. **Workflow parser** (`src/workflow/parser/`) - mdast-based markdown parser with validation
4. **State manager** (`src/workflow/state.ts`) - JSON file persistence, session tracking
5. **CLI** (`src/cli/workflow-cli.ts`) - start, next, status, list, stop commands
6. **Hook integration** (`src/workflow/hooks/`, `src/workflow/context.ts`) - context injection, subagent tracking
7. **Documentation** - Comprehensive README with ~1200 lines of workflow documentation

**Highlights (Examples of Quality Code):**

- **Excellent type safety:** The use of branded types for `TaskNumber` and discriminated unions for `Action` prevents invalid states at compile time. This is textbook TypeScript.

- **Clean parser architecture:** Using mdast-util-from-markdown for AST-based parsing is a solid choice that mirrors the Rust pulldown-cmark pattern mentioned in comments. The parser validates sequentially-numbered tasks and GOTO targets.

- **Comprehensive validation:** The parser validates workflows at parse time (empty workflows, non-sequential numbering, invalid GOTO targets, self-referential GOTOs). This fail-fast approach catches errors early.

- **Good separation of concerns:** The workflow system is cleanly separated into types, parser, state manager, context, and hooks. Each module has a single responsibility.

- **Defensive programming:** The `gateMatchesFilePattern` function handles edge cases (undefined file_path, already-relative paths, empty patterns) gracefully. Error handling logs warnings but doesn't crash.

- **Thorough test coverage:** 220 tests covering workflow parsing, state management, CLI integration, context injection, and hook integration. Tests verify behavior across edge cases.

- **Clear documentation:** The README provides comprehensive documentation including syntax reference, best practices, and migration guide. The execution paradigm ("Claude executes, workflow tracks") is well-explained.
