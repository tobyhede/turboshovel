---
name: Code Review Template
description: Structured format for saving code review feedback.
when_to_use: when conducting code reviews and saving structured feedback to work directories
version: 1.0.0
---

# Code Review - 2025-12-23

## Status: BLOCKED

<!--
Status guidance:
- BLOCKED: Has BLOCKING issues that must be fixed before merge
- APPROVED WITH (NON-BLOCKING) SUGGESTIONS: Ready to merge, but consider addressing suggestions
- APPROVED: Clean, ready to merge with no issues

Note: Tests and checks are assumed to pass. This review focuses on code quality.
-->

## BLOCKING (Must Fix Before Merge)

**Incomplete refactoring breaks TypeScript compilation:**
- Description: The Step → Task rename was only partially completed in these 3 commits. While types.ts, parser/types.ts, and parser/helpers.ts were updated, several dependent files still reference the old names, causing 16 TypeScript compilation errors.
- Location: Multiple files - see details below
- Action: Complete the refactoring by updating all remaining files that reference the old Step/StepNumber/createStepNumber/extractStepHeader names. Files requiring updates:
  - `src/cli/workflow-cli.ts` (8 errors)
  - `src/workflow/context.ts` (2 errors)
  - `src/workflow/parser/parser.ts` (4 errors)
  - `src/workflow/state.ts` (2 errors)

**Specific compilation errors to fix:**

1. **src/cli/workflow-cli.ts**
   - Line 9: Import `createTaskNumber` instead of `createStepNumber`
   - Line 9: Import `Task` instead of `Step`
   - Line 92: Change `state.step` → `state.task`
   - Line 111: Change `step:` → `task:`
   - Line 170: Change `state.step` → `state.task` and `state.stepName` → `state.taskName`
   - Line 229: Change `state.step` → `state.task`
   - Line 261: Change `action.step` → `action.task`

2. **src/workflow/context.ts**
   - Line 25: Change `state.step` → `state.task` and `state.stepName` → `state.taskName`

3. **src/workflow/parser/parser.ts**
   - Line 6: Import `Task` instead of `Step`
   - Line 6: Import `TaskNumber` instead of `StepNumber`
   - Line 7: Import `extractTaskHeader` instead of `extractStepHeader`
   - Line 245: Change `action.step` → `action.task`

4. **src/workflow/state.ts**
   - Line 4: Import `createTaskNumber` instead of `createStepNumber`
   - Line 42: Change `step:` → `task:`

**Missing systematic approach to ensure completeness:**
- Description: A systematic refactoring of this scope should include searching the entire codebase for all references to old names before beginning changes. The partial nature of this refactoring suggests the changes were made incrementally without a comprehensive plan.
- Location: All workflow-related files
- Action: Before continuing, run `grep -r "StepNumber\|createStepNumber\|extractStepHeader\|\.step\|\.steps\|stepName" plugin/hooks/hooks-app/src/` to identify ALL remaining references. Update all files atomically or in a clear dependency order (types → helpers → consumers).


## NON-BLOCKING (May Be Deferred)

**Test description strings still reference "step":**
- Description: In helpers.test.ts, test descriptions use "step" in string literals like `'parses "1. First step"'` and `'rejects Step keyword'`. While these are just descriptive strings and don't affect functionality, updating them would improve consistency.
- Location: `plugin/hooks/hooks-app/__tests__/workflow/parser/helpers.test.ts:10,18,22,52-53`
- Action: Consider updating test description strings to use "task" terminology for consistency:
  - Line 10: `'First step'` → `'First task'`
  - Line 18: `'First step'` → `'First task'`
  - Line 22: `'First step'` → `'First task'`
  - Lines 52-53: Keep "Step" in the test name as it's validating rejection of old terminology

**Intentional guard against old "Step" keyword is well-designed:**
- Description: The guard in `extractTaskHeader` that rejects "Step ..." patterns (line 44) is good defensive programming. The comment explaining this is clear.
- Location: `plugin/hooks/hooks-app/src/workflow/parser/helpers.ts:42-44`
- Action: No action needed - this is a positive observation. Consider similar guards in validation code elsewhere.

**Variable naming could be more consistent:**
- Description: In parseAction function, the intermediate variables are named `taskStr` and `taskNum` which is good, but similar clarity could be applied elsewhere.
- Location: `plugin/hooks/hooks-app/src/workflow/parser/helpers.ts:79-80`
- Action: This is already well done - consider this pattern for future variable naming.


## Checklist

**Security & Correctness:**
- [ ] No security vulnerabilities (SQL injection, XSS, CSRF, exposed secrets)
- [x] No insecure dependencies or deprecated cryptographic functions
- [ ] **No critical logic bugs (meets acceptance criteria)** - Refactoring incomplete, breaks TypeScript compilation
- [x] No race conditions, deadlocks, or data races
- [x] No unhandled errors, rejected promises, or panics
- [x] No breaking API or schema changes without migration plan

**Testing:**
- [x] All tests passing (unit, integration, property-based where applicable)
- [x] New logic has corresponding tests
- [x] Tests cover edge cases and error conditions
- [x] Tests verify behavior (not implementation details)
- [x] Property-based tests for mathematical/algorithmic code with invariants
- [x] Tests are isolated (independent, don't rely on other tests)
- [x] Test names are clear and use structured arrange-act-assert patterns

**Architecture:**
- [x] Single Responsibility Principle (functions/files have one clear purpose)
- [x] No non-trivial duplication (logic that if changed in one place would need changing elsewhere)
- [x] Clean separation of concerns (business logic separate from data marshalling)
- [x] No leaky abstractions (internal details not exposed)
- [x] No over-engineering (YAGNI - implement only current requirements)
- [x] No tight coupling (excessive dependencies between modules)
- [x] Proper encapsulation (internal details not exposed across boundaries)
- [x] Modules can be understood and tested in isolation

**Error Handling:**
- [x] No swallowed exceptions or silent failures
- [x] Error messages provide sufficient context for debugging
- [x] Fail-fast on invariants where appropriate

**Code Quality:**
- [x] Simple, not clever (straightforward solutions over complex ones)
- [x] Clear, descriptive naming (variables, functions, classes)
- [ ] **Type safety maintained** - Compilation fails due to incomplete refactoring
- [x] Follows language idioms and project patterns consistently
- [x] No magic numbers or hardcoded strings (use named constants)
- [x] Consistent approaches when similar functionality exists elsewhere
- [x] Comments explain "why" not "what" (code should be self-documenting)
- [x] Rationale provided for non-obvious design decisions
- [x] Doc comments for public APIs

**Process:**
- [x] No obvious performance issues (N+1 queries, inefficient algorithms on hot paths)
- [x] ALL linter warnings addressed by fixing root cause (disable/allow/ignore ONLY when unavoidable)
- [ ] **Requirements met exactly (no scope creep)** - Refactoring incomplete
- [x] No unnecessary reinvention (appropriate use of existing libraries/patterns)


## Next Steps

1. **Address BLOCKING issues:**
   - Do NOT merge these 3 commits until the refactoring is completed
   - Run comprehensive search for all remaining Step/step references
   - Update all 4 files identified above (cli, context, parser, state)
   - Verify TypeScript compilation succeeds: `cd plugin/hooks/hooks-app && npx tsc --noEmit`
   - Verify all tests still pass: `cd plugin/hooks/hooks-app && npm test`

2. **Consider NON-BLOCKING suggestions:**
   - Update test description strings for consistency
   - Review approach to systematic refactoring for future changes

3. **Merge strategy:**
   - Once completed, consider squashing these 3 commits plus the completion commits into a single atomic commit
   - Alternative: Add completion commits with clear references to this review


## Additional Context

**Review scope:** Last 3 commits on current branch
- `1b2a4f0` - refactor(workflow): rename Step to Task in parser helpers
- `69d7fb2` - refactor(workflow): update parser types imports for Task rename
- `d339603` - refactor(workflow): rename Step to Task in core types

**Files changed in scope:**
- `plugin/hooks/hooks-app/src/workflow/types.ts`
- `plugin/hooks/hooks-app/__tests__/workflow/types.test.ts`
- `plugin/hooks/hooks-app/src/workflow/parser/types.ts`
- `plugin/hooks/hooks-app/src/workflow/parser/helpers.ts`
- `plugin/hooks/hooks-app/__tests__/workflow/parser/helpers.test.ts`

**Files requiring updates (not in commits):**
- `plugin/hooks/hooks-app/src/cli/workflow-cli.ts`
- `plugin/hooks/hooks-app/src/workflow/context.ts`
- `plugin/hooks/hooks-app/src/workflow/parser/parser.ts`
- `plugin/hooks/hooks-app/src/workflow/state.ts`

**Verification commands run:**
```bash
# View commits
git log -3 --stat

# Check diff
git diff d33960365fab1ab8c534a61784645c2e12eff382^..1b2a4f0f19ca5fcb7e52ae409b1a7b0f398be81a

# Search for remaining references
grep -n "Step\|step" plugin/hooks/hooks-app/src/workflow/types.ts plugin/hooks/hooks-app/__tests__/workflow/types.test.ts plugin/hooks/hooks-app/src/workflow/parser/types.ts plugin/hooks/hooks-app/src/workflow/parser/helpers.ts plugin/hooks/hooks-app/__tests__/workflow/parser/helpers.test.ts

# Run tests
cd plugin/hooks/hooks-app && npm test -- --testPathPattern="workflow/(types|parser/helpers)"

# Check TypeScript compilation
cd plugin/hooks/hooks-app && npx tsc --noEmit --project tsconfig.json
```

**Test results:** 35 tests passed (types.test.ts and helpers.test.ts)

**TypeScript compilation:** 16 errors (see BLOCKING section above)
