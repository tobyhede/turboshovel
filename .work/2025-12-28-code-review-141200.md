# Code Review - 2025-12-28

## Status: APPROVED WITH SUGGESTIONS

## BLOCKING (Must Fix Before Merge)

None

## NON-BLOCKING (May Be Deferred)

**Use of `any` type in CLI gate command execution:**
- Description: Both `/Users/tobyhede/psrc/turboshovel/packages/cli/src/cli.ts:607` and `/Users/tobyhede/psrc/turboshovel/plugin/core/src/cli/workflow-cli.ts:610` use `const options: any` for `execSync` options. While this works, it loses type safety.
- Location: `packages/cli/src/cli.ts:607`, `plugin/core/src/cli/workflow-cli.ts:610`
- Action: Replace `any` with proper `ExecSyncOptions` type from `child_process`:
  ```typescript
  import { execSync, ExecSyncOptions } from 'child_process';
  const options: ExecSyncOptions = { cwd, stdio: 'inherit', shell: true };
  ```

**Use of `any` in AST visitor callback:**
- Description: In `/Users/tobyhede/psrc/turboshovel/packages/shared/src/workflow/parser/parser.ts:99`, the `visit()` callback uses `(node: any, index: any, parent: any)`. While the mdast types are complex, proper typing improves maintainability.
- Location: `packages/shared/src/workflow/parser/parser.ts:99`
- Action: Import and use proper types from unist/mdast:
  ```typescript
  import type { Node } from 'unist';
  visit(tree, (node: Node, index: number | undefined, parent: Node | undefined) => { ... });
  ```
  Or use a type assertion inside the callback where specific node types are needed.

**Unchecked type assertion on WorkflowState load:**
- Description: In `/Users/tobyhede/psrc/turboshovel/packages/shared/src/workflow/state.ts:103`, `JSON.parse(content) as WorkflowState` assumes the parsed JSON matches the interface without validation. If the file is corrupted or has a schema mismatch, this could cause runtime errors.
- Location: `packages/shared/src/workflow/state.ts:103`
- Action: Add runtime validation using Zod schema similar to SessionState validation in `/Users/tobyhede/psrc/turboshovel/plugin/core/src/session.ts:98`. Create a `WorkflowStateSchema` and use `safeParse()`.

**Unchecked type assertion in gate-loader:**
- Description: In `/Users/tobyhede/psrc/turboshovel/plugin/core/src/gate-loader.ts:47-53`, the error caught from `execAsync` is cast to an ad-hoc type. While this handles the ExecException properties, it could be cleaner with a proper type guard.
- Location: `plugin/core/src/gate-loader.ts:47-53`
- Action: Create a type guard for ExecException:
  ```typescript
  interface ExecError extends Error {
    killed?: boolean;
    signal?: string;
    code?: number;
    stdout?: string;
    stderr?: string;
  }
  function isExecError(error: unknown): error is ExecError {
    return error instanceof Error;
  }
  ```

**Logger LogLevel assertion:**
- Description: In `/Users/tobyhede/psrc/turboshovel/packages/shared/src/logger.ts:54`, `process.env.TURBOSHOVEL_LOG_LEVEL as LogLevel` assumes the env var contains a valid log level string. While the subsequent check validates this, the initial cast is unsafe.
- Location: `packages/shared/src/logger.ts:54`
- Action: Remove the assertion and use a type guard pattern:
  ```typescript
  const envLevel = process.env.TURBOSHOVEL_LOG_LEVEL;
  if (envLevel && LOG_LEVELS[envLevel as LogLevel] !== undefined) {
    return envLevel as LogLevel;
  }
  ```
  Or better: validate with Zod `z.enum(['debug', 'info', 'warn', 'error']).optional()`.

**Missing explicit return types on some functions:**
- Description: Some public API functions lack explicit return type annotations. While TypeScript infers them, explicit types improve API documentation and catch unintended changes.
- Location: Various files including `findWorkflowFile()` in CLI files
- Action: Add explicit return types to public functions like `async function findWorkflowFile(cwd: string, filename: string): Promise<string | null>` (already has it, but verify others).


## Checklist

**Security & Correctness:**
- [x] No security vulnerabilities (SQL injection, XSS, CSRF, exposed secrets)
- [x] No insecure dependencies or deprecated cryptographic functions
- [x] No critical logic bugs (meets acceptance criteria)
- [x] No race conditions, deadlocks, or data races
- [x] No unhandled errors, rejected promises, or panics
- [x] No breaking API or schema changes without migration plan

**Testing:**
- [x] All tests passing (unit, integration, property-based where applicable)
- [x] New logic has corresponding tests
- [x] Tests cover edge cases and error conditions
- [x] Tests verify behavior (not implementation details)
- [x] Property-based tests for mathematical/algorithmic code with invariants - Excellent use of fast-check for TaskId, SessionState, and HookInput validation
- [x] Tests are isolated (independent, don't rely on other tests)
- [x] Test names are clear and use structured arrange-act-assert patterns

**Architecture:**
- [x] Single Responsibility Principle (functions/files have one clear purpose)
- [x] No non-trivial duplication (logic that if changed in one place would need changing elsewhere)
- [x] Clean separation of concerns (business logic separate from data marshalling)
- [x] No leaky abstractions (internal details not exposed)
- [x] No over-engineering (YAGNI - implement only current requirements)
- [x] No tight coupling (excessive dependencies between modules)
- [x] Proper encapsulation (internal details not exposed across boundaries)
- [x] Modules can be understood and tested in isolation

**Error Handling:**
- [x] No swallowed exceptions or silent failures
- [x] Error messages provide sufficient context for debugging
- [x] Fail-fast on invariants where appropriate

**Code Quality:**
- [x] Simple, not clever (straightforward solutions over complex ones)
- [x] Clear, descriptive naming (variables, functions, classes)
- [x] Type safety maintained - Excellent use of branded types (TaskNumber), discriminated unions (Action, Conditions, SessionLoadError), type guards, and Zod schemas
- [x] Follows language idioms and project patterns consistently
- [x] No magic numbers or hardcoded strings (use named constants) - MAX_GATES_PER_DISPATCH, MAX_TASK_NUMBER, DESCRIPTION_DISPLAY_LIMIT defined appropriately
- [x] Consistent approaches when similar functionality exists elsewhere
- [x] Comments explain "why" not "what" (code should be self-documenting)
- [x] Rationale provided for non-obvious design decisions - Excellent JSDoc comments throughout
- [x] Doc comments for public APIs

**Process:**
- [x] No obvious performance issues (N+1 queries, inefficient algorithms on hot paths)
- [x] ALL linter warnings addressed by fixing root cause (disable/allow/ignore ONLY when unavoidable)
- [x] Requirements met exactly (no scope creep)
- [x] No unnecessary reinvention (appropriate use of existing libraries/patterns) - Good use of Zod, minimatch, commander, mdast

## Next Steps

1. Address BLOCKING issues (if any) - None
2. Consider NON-BLOCKING suggestions - The type safety improvements listed above are optional but would strengthen the codebase
3. Ready to merge when status is APPROVED or APPROVED WITH SUGGESTIONS

---

## Review Details

**Git Range:**
- Base: `670ead13a0cfef22f421ce9c800593cece298e07`
- Head: `6d453e7b8770382f8bff319c70a5dd49adf04ea8`
- Commits reviewed: 114 commits
- Files changed: 339 files

**Review Focus:** Idiomatic TypeScript and pervasive use of types for strong type safety

## Type Safety Highlights (Positive Observations)

The codebase demonstrates excellent TypeScript practices overall:

1. **Branded Types for Domain Values**
   - `TaskNumber` branded type in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/workflow/types.ts:8` prevents mixing task numbers with regular numbers
   - Factory function `createTaskNumber()` validates and brands at boundaries
   - Arithmetic helpers `incrementTaskNumber()` and `decrementTaskNumber()` preserve brands

2. **Discriminated Unions for State Machines**
   - `Action` type (`CONTINUE | STOP | GOTO | DONE | RETRY`) in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/workflow/types.ts:51-56`
   - `Conditions` type with `all` discriminant for aggregation modes in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/workflow/types.ts:65-77`
   - `SessionLoadError` discriminated union for error handling in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/errors.ts:29-32`
   - `ParseResult<T>` success/failure discriminant in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/schemas.ts:46`

3. **Type Guards for Safe Error Handling**
   - `isNodeError()`, `isError()`, `getErrorMessage()` in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/errors.ts`
   - `isFileNotFoundError()` for discriminated union narrowing

4. **Zod Schemas at System Boundaries**
   - `HookInputSchema` validates external CLI input in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/schemas.ts:17-39`
   - `SessionStateSchema` with defaults for persisted state in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/schemas.ts:83-94`
   - Type inference with `z.infer<>` for DRY type definitions

5. **Const Assertions with `satisfies`**
   - `SESSION_STATE_KEYS` in `/Users/tobyhede/psrc/turboshovel/packages/shared/src/types.ts:101-109` uses `as const satisfies readonly (keyof SessionState)[]` for compile-time key validation

6. **Property-Based Testing for Type Invariants**
   - `/Users/tobyhede/psrc/turboshovel/plugin/core/__tests__/workflow/task-id.properties.test.ts` - Roundtrip testing for serialization
   - `/Users/tobyhede/psrc/turboshovel/plugin/core/__tests__/schemas.properties.test.ts` - Schema validation invariants
   - `/Users/tobyhede/psrc/turboshovel/plugin/core/__tests__/session.properties.test.ts` - Session state properties

7. **Readonly Types for Immutability**
   - Extensive use of `readonly` on interface properties throughout workflow types
   - `readonly Task[]`, `readonly Subtask[]`, `readonly TaskId[]` arrays prevent mutation

8. **Explicit Nullability**
   - Functions return `T | null` explicitly (e.g., `createTaskNumber`, `parseTaskIdFromString`)
   - `active_command: string | null` in SessionState

9. **Generic Result Types**
   - `SessionLoadResult<T>` and `ParseResult<T>` for consistent error handling patterns

10. **Exhaustive Switch Statements**
    - `evaluateConditions()` in `/Users/tobyhede/psrc/turboshovel/plugin/core/src/workflow/evaluation.ts` uses exhaustive switch on `conditions.all` boolean discriminant
    - Switch statements on `Action.type` in CLI commands handle all cases

The implementation shows mature TypeScript usage with only minor areas where `any` usage could be replaced with proper types.
