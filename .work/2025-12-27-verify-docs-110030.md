# Review - 2025-12-27

## Metadata
- **Reviewer:** technical-writer (independent verification agent)
- **Date:** 2025-12-27 11:00:30
- **Subject:** User-facing documentation verification for turboshovel plugin
- **Ground Truth:** Codebase implementation (packages/cli/src/cli.ts, plugin/core/src/schemas.ts, plugin/hooks.json, plugin/gates.json, actual file system structure)
- **Context:** Independent review #1 for dual-verification
- **Mode:** Review

## Summary
- **Subject:** Verification of all user-facing documentation files against actual codebase implementation
- **Scope:** README.md, CLAUDE.md, SETUP.md, CONVENTIONS.md, TYPESCRIPT.md, ARCHITECTURE.md, packages/cli/README.md, INTEGRATION_TESTS.md

---

## Status: APPROVED WITH SUGGESTIONS

## BLOCKING (Must Address)

**B1: CLAUDE.md CLI command syntax inconsistent with implementation:**
- Description: CLAUDE.md documents `tsv next --fail` but does not document `tsv next --pass` which is implemented in cli.ts
- Location: /Users/tobyhede/psrc/turboshovel/CLAUDE.md:107-108
- Impact: Users may not discover the `--pass` flag, though this appears to be intentional since the default is to pass
- Action: Consider if documenting `--pass` is needed for completeness. The actual CLI has both `--pass` and `--fail` flags.
- **Severity downgrade to SUGGESTION**: Upon closer review, the default behavior without flags is to advance (which is effectively pass), so `--pass` flag documentation is optional.

None identified as BLOCKING.

## SUGGESTIONS (Would Improve Quality)

**S1: ARCHITECTURE.md directory structure diagram is incomplete:**
- Description: The directory structure diagram in ARCHITECTURE.md shows `plugin/core/src/cli/workflow-cli.ts` but omits `condition-handler.ts` which also exists in that directory. The diagram also shows files in `plugin/core/src/workflow/hooks/` but omits `substitute.ts`.
- Location: /Users/tobyhede/psrc/turboshovel/ARCHITECTURE.md:97-146
- Benefit: Complete accuracy of directory structure would help developers navigate the codebase
- Action: Add missing files to directory structure:
  - `plugin/core/src/cli/condition-handler.ts`
  - `plugin/core/src/workflow/hooks/substitute.ts`

**S2: README.md Debugging section CLI path may be confusing:**
- Description: README.md shows `node ${CLAUDE_PLUGIN_ROOT}/core/hooks-app/dist/cli.js log-path` but the actual path is `${CLAUDE_PLUGIN_ROOT}/core/dist/cli.js` (no "hooks-app" subdirectory)
- Location: /Users/tobyhede/psrc/turboshovel/README.md:420-421
- Benefit: Correct CLI path prevents user confusion
- Action: Update the commented alternative to: `node ${CLAUDE_PLUGIN_ROOT}/core/dist/cli.js log-path`

**S3: INTEGRATION_TESTS.md references incorrect hooks.json location:**
- Description: INTEGRATION_TESTS.md mentions `${CLAUDE_PLUGIN_ROOT}/core/hooks.json` in Troubleshooting section, but hooks.json is at `plugin/hooks.json`, not `plugin/core/hooks.json`
- Location: /Users/tobyhede/psrc/turboshovel/INTEGRATION_TESTS.md:275
- Benefit: Correct path helps users troubleshoot issues
- Action: Change `${CLAUDE_PLUGIN_ROOT}/core/hooks.json` to `${CLAUDE_PLUGIN_ROOT}/hooks.json`

**S4: TYPESCRIPT.md shows src/gates/ path but actual location is plugin/core/src/gates/:**
- Description: TYPESCRIPT.md consistently uses `src/gates/` without the full path context. While understandable in context, it could be clearer.
- Location: /Users/tobyhede/psrc/turboshovel/TYPESCRIPT.md:7, 35
- Benefit: Absolute paths reduce ambiguity for new developers
- Action: Consider updating to `plugin/core/src/gates/` for clarity or adding a note about the base path

**S5: README.md and CLAUDE.md document different CLI commands:**
- Description: CLAUDE.md (line 107) documents `tsv next --pass` but README.md (line 558-560) does not mention `--pass` flag. CLI implementation includes both `--pass` and `--fail` flags.
- Location: CLAUDE.md:107-108, README.md:558-560
- Benefit: Consistent documentation across files
- Action: Ensure both files document the same CLI options. The CLI implements: `--step`, `--pass`, `--fail`, `--retry`, `--task`, `--agent`

**S6: packages/cli/README.md mentions --fail but not --pass:**
- Description: CLI README documents `tsv next --fail` but not the corresponding `--pass` flag
- Location: /Users/tobyhede/psrc/turboshovel/packages/cli/README.md:23-24
- Benefit: Complete documentation of CLI options
- Action: Consider adding `--pass` documentation for symmetry, or noting that it's the default behavior

**S7: ARCHITECTURE.md shows 11 hooks but planned hooks section unclear:**
- Description: ARCHITECTURE.md mentions "Planned hooks (not yet registered): SlashCommandStart, SlashCommandEnd, SkillStart, SkillEnd" but these same hooks are also mentioned in README.md. The distinction between registered and planned is clear but could be more explicit.
- Location: /Users/tobyhede/psrc/turboshovel/ARCHITECTURE.md:247
- Benefit: Clear distinction helps users understand what's available now vs later
- Action: No change required, just noting for awareness

**S8: Examples directory has convention-based.json not mentioned in docs:**
- Description: The examples directory contains `convention-based.json` which is not documented in any of the documentation files
- Location: /Users/tobyhede/psrc/turboshovel/examples/convention-based.json
- Benefit: Documenting all available examples helps users discover options
- Action: Consider adding to the Examples section in README.md or removing if deprecated

**S9: SETUP.md test command path uses old structure:**
- Description: SETUP.md shows `node ${CLAUDE_PLUGIN_ROOT}/core/dist/cli.js` which is correct for the plugin core CLI, but the workflow CLI is now separate at `packages/cli`
- Location: /Users/tobyhede/psrc/turboshovel/SETUP.md:391-392
- Benefit: Clarity about which CLI is being referenced
- Action: Add context that this tests the hook processing CLI, not the workflow CLI

---

## Verification Details

### File Existence Verification

| Path | Status |
|------|--------|
| examples/strict.json | EXISTS |
| examples/permissive.json | EXISTS |
| examples/pipeline.json | EXISTS |
| examples/context/ | EXISTS |
| examples/code-review.workflow.md | EXISTS |
| plugin/hooks.json | EXISTS |
| plugin/gates.json | EXISTS |
| plugin/core/src/gates/index.ts | EXISTS |
| plugin/core/src/gates/plugin-path.ts | EXISTS |
| plugin/core/src/cli/workflow-cli.ts | EXISTS |
| plugin/core/src/workflow/ | EXISTS |
| plugin/core/src/workflow/parser/ | EXISTS |
| plugin/core/src/workflow/hooks/ | EXISTS |
| packages/cli/src/cli.ts | EXISTS |

### CLI Commands Verification

| Documented Command | Implementation Status |
|-------------------|----------------------|
| `tsv start <file>` | CORRECT - `program.command('start [file]')` |
| `tsv next` | CORRECT |
| `tsv next --step N` | CORRECT - `--step <n>` option exists |
| `tsv next --fail` | CORRECT - `--fail` option exists |
| `tsv next --pass` | EXISTS IN CODE but not fully documented in README |
| `tsv next --retry` | CORRECT - `--retry` option exists |
| `tsv status` | CORRECT |
| `tsv stop` | CORRECT |
| `tsv complete` | CORRECT |
| `tsv stash` | CORRECT |
| `tsv pop` | CORRECT |
| `tsv list` | CORRECT |
| `tsv gate <name>` | CORRECT |

### Hook Registration Verification

All 11 hooks documented in README.md are registered in plugin/hooks.json:
- SessionStart, SessionEnd, UserPromptSubmit, SubagentStart, SubagentStop
- PreToolUse, PostToolUse, Stop, Notification, PreCompact, PermissionRequest

### Gates Configuration Verification

plugin/gates.json contains gates matching documentation:
- `plugin-path` (TypeScript gate, no command)
- `check`, `test`, `build` (shell command gates with placeholder commands)
- `walkthrough:log`, `walkthrough:gate-check` (testing gates)

### Directory Structure Verification

ARCHITECTURE.md directory structure largely matches actual codebase with minor omissions noted in suggestions.

---

## Assessment

**Conclusion:**
Documentation is largely accurate and well-maintained. Most paths, commands, and configurations match the implementation. A few minor path references and undocumented files exist but do not significantly impact user experience. The documentation correctly describes the dual CLI architecture (plugin core CLI for hook processing, packages/cli for workflow orchestration).

**Confidence in findings:**
HIGH - Verified against actual file system, source code, and configuration files. All major claims in documentation were checked against ground truth sources.
