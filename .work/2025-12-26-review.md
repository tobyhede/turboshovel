# Code Review - 2025-12-26

## Status: BLOCKED

## BLOCKING (Must Fix Before Merge)

**Missing Critical Dependencies:**
- Description: `config.ts` imports `logger`, `utils`, and `types` from plugin/core but these files were not copied to packages/cli. This will cause immediate TypeScript compilation failures and runtime errors.
- Location: packages/cli/src/config.ts:4-6
- Action: Copy the following missing files from plugin/core/src/ to packages/cli/src/: `utils.ts`, `logger.ts`, and `types.ts`. Verify imports resolve correctly by running `cd packages/cli && npx tsc --noEmit`.

**Broken Module References:**
- Description: The copied `config.ts` contains file path comment referencing the old location `// plugin/hooks/hooks-app/src/config.ts` which will confuse developers about the canonical location.
- Location: packages/cli/src/config.ts:1, packages/cli/src/errors.ts (if similar comments exist)
- Action: Remove or update all `// plugin/hooks/hooks-app/` path comments in copied files to reflect the new location `// packages/cli/src/`. Run `grep -r "plugin/hooks/hooks-app" packages/cli/src/` to find all occurrences.

**Missing CLI Entrypoint:**
- Description: Task 1-3 were implemented but the main CLI entrypoint (`cli.ts`) specified in Task 2 Step 3 of the plan is completely missing. The package.json points to `dist/cli.js` as the main entrypoint and bin commands, but this file does not exist.
- Location: packages/cli/src/ (file missing), packages/cli/package.json:5,7-8
- Action: This is outside Batch 1 scope (belongs to Task 2 Step 3 and Task 4), but must be flagged as the package is currently non-functional. Future batches must implement the complete cli.ts file per the plan.

**Incorrect Import Paths in Workflow Files:**
- Description: Workflow files (state.ts, task-id.ts, parser files) import from relative paths like `./types` which assumes they're in the same directory structure as plugin/core. Since config.ts, schemas.ts, and errors.ts are now in `packages/cli/src/` not `packages/cli/src/workflow/`, these imports need path adjustments.
- Location: packages/cli/src/workflow/state.ts:4, packages/cli/src/workflow/task-id.ts:1, packages/cli/src/workflow/parser/parser.ts:23, packages/cli/src/workflow/parser/helpers.ts:4-5
- Action: Audit all import statements in workflow/ subdirectory. Update relative imports to correctly reference parent directory files (e.g., `import { ... } from '../types'` if types.ts is in src/ not src/workflow/).

## NON-BLOCKING (May Be Deferred)

**Inconsistent File Organization:**
- Description: The plan specifies copying workflow/, errors.ts, config.ts, schemas.ts but config.ts/schemas.ts are gate-related files not directly needed for CLI workflow orchestration. This creates confusion about which files are actually required for the CLI vs plugin-specific.
- Location: packages/cli/src/config.ts, packages/cli/src/schemas.ts
- Action: Future refactoring should clarify which files are genuinely needed for the CLI package. Consider creating a dependency map or splitting into @turboshovel/core and @turboshovel/cli packages if significant overlap exists.

**Missing TypeScript Configuration Validation:**
- Description: tsconfig.json uses "module": "NodeNext" and "moduleResolution": "NodeNext" but there's no verification that all imports use explicit file extensions (.js) as required by NodeNext/ESM. This can cause runtime module resolution failures.
- Location: packages/cli/tsconfig.json:4-5
- Action: Add explicit file extensions to all imports (e.g., `import { x } from './utils.js'`) or switch to "module": "CommonJS" if ESM is not required. Test with `npm run build` to verify module resolution.

**Unclear Dependency Versions:**
- Description: Dependencies use caret ranges (^14.0.2, ^4.1.1, etc.) which could introduce breaking changes. For a CLI tool meant to be globally installed, more precise version pinning may reduce "works on my machine" issues.
- Location: packages/cli/package.json:26-32
- Action: Consider using exact versions (remove ^) for critical dependencies or add package-lock.json to repository. Document dependency version selection rationale in README.

**No Entry Point Validation:**
- Description: package.json specifies both "main" and "bin" fields pointing to dist/cli.js but there's no shebang verification or executable permission check in the build process.
- Location: packages/cli/package.json:5,7-8
- Action: Ensure cli.ts starts with `#!/usr/bin/env node` shebang (per plan Task 2 Step 3). Add post-build script to verify dist/cli.js is executable: `"postbuild": "chmod +x dist/cli.js"`.

**Zod Version Mismatch:**
- Description: package.json specifies `"zod": "^4.2.1"` but Zod's latest stable version is 3.x (as of knowledge cutoff). Version 4.x may not exist or could be a typo.
- Location: packages/cli/package.json:32
- Action: Verify intended Zod version. If 3.x is correct, change to `"zod": "^3.22.0"` or appropriate 3.x version. Check plugin/core/package.json for consistency.

## Checklist

**Security & Correctness:**
- [x] No security vulnerabilities (SQL injection, XSS, CSRF, exposed secrets)
- [x] No insecure dependencies or deprecated cryptographic functions
- [ ] No critical logic bugs (meets acceptance criteria) - Missing dependencies prevent compilation
- [x] No race conditions, deadlocks, or data races
- [x] No unhandled errors, rejected promises, or panics
- [x] No breaking API or schema changes without migration plan

**Testing:**
- [ ] All tests passing (unit, integration, property-based where applicable) - No tests exist yet
- [ ] New logic has corresponding tests - Not applicable for Batch 1 (scaffolding only)
- [ ] Tests cover edge cases and error conditions
- [ ] Tests verify behavior (not implementation details)
- [ ] Property-based tests for mathematical/algorithmic code with invariants
- [ ] Tests are isolated (independent, don't rely on other tests)
- [ ] Test names are clear and use structured arrange-act-assert patterns

**Architecture:**
- [x] Single Responsibility Principle (functions/files have one clear purpose)
- [x] No non-trivial duplication (logic that if changed in one place would need changing elsewhere)
- [x] Clean separation of concerns (business logic separate from data marshalling)
- [x] No leaky abstractions (internal details not exposed)
- [x] No over-engineering (YAGNI - implement only current requirements)
- [x] No tight coupling (excessive dependencies between modules)
- [x] Proper encapsulation (internal details not exposed across boundaries)
- [x] Modules can be understood and tested in isolation

**Error Handling:**
- [x] No swallowed exceptions or silent failures
- [x] Error messages provide sufficient context for debugging
- [x] Fail-fast on invariants where appropriate

**Code Quality:**
- [x] Simple, not clever (straightforward solutions over complex ones)
- [x] Clear, descriptive naming (variables, functions, classes)
- [x] Type safety maintained
- [x] Follows language idioms and project patterns consistently
- [x] No magic numbers or hardcoded strings (use named constants)
- [x] Consistent approaches when similar functionality exists elsewhere
- [ ] Comments explain "why" not "what" (code should be self-documenting) - Stale path comments exist
- [x] Rationale provided for non-obvious design decisions
- [x] Doc comments for public APIs

**Process:**
- [x] No obvious performance issues (N+1 queries, inefficient algorithms on hot paths)
- [x] ALL linter warnings addressed by fixing root cause (disable/allow/ignore ONLY when unavoidable)
- [ ] Requirements met exactly (no scope creep) - Missing critical files prevent meeting requirements
- [x] No unnecessary reinvention (appropriate use of existing libraries/patterns)

## Next Steps

1. Address BLOCKING issues (missing dependencies, broken imports, stale comments)
2. Verify TypeScript compilation succeeds: `cd packages/cli && npm install && npx tsc --noEmit`
3. Consider NON-BLOCKING suggestions (Zod version, module resolution strategy)
4. Ready to proceed to Batch 2 (Task 4: Complete CLI implementation) when status is APPROVED

---

## Review Context

**Commits Reviewed:**
- BASE: f64c50c (chore: move examples to project root)
- HEAD: f6e9d96 (feat: add condition-handler to packages/cli)

**Commits in Range:**
1. 3339945 - chore: scaffold packages/cli directory
2. e1877e7 - feat: copy workflow source files to packages/cli
3. f6e9d96 - feat: add condition-handler to packages/cli

**Files Changed (14 files, +1780 lines):**
- packages/cli/.gitignore
- packages/cli/package.json
- packages/cli/tsconfig.json
- packages/cli/src/config.ts
- packages/cli/src/errors.ts
- packages/cli/src/schemas.ts
- packages/cli/src/workflow/condition-handler.ts
- packages/cli/src/workflow/parser/ (4 files)
- packages/cli/src/workflow/state.ts
- packages/cli/src/workflow/task-id.ts
- packages/cli/src/workflow/types.ts

**Plan Reference:**
Plan document: `.work/npm-publish/2025-12-26-cli-npm-package.md`
Tasks completed: 1 (scaffold), 2 (copy workflow sources), 3 (add condition-handler)

**Verification Commands Run:**
```bash
git log f64c50c..f6e9d96 --oneline
git diff f64c50c..f6e9d96 --stat
git diff f64c50c..f6e9d96
grep -r "^import.*from ['\"]\./(utils|logger|types)['\"]" packages/cli/src/
```
