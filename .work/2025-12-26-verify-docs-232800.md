# Review - 2025-12-26

## Metadata
- **Reviewer:** technical-writer
- **Date:** 2025-12-26 23:28:00
- **Subject:** User-facing documentation verification (README.md, CLAUDE.md, SETUP.md, CONVENTIONS.md, TYPESCRIPT.md, packages/cli/README.md)
- **Ground Truth:** Current codebase implementation
- **Context:** Independent review #1 for dual-verification
- **Mode:** Review

## Summary
- **Subject:** Verification of all user-facing documentation against actual codebase
- **Scope:** File paths, CLI commands, configuration examples, feature claims, cross-references

---

## Status: BLOCKED

## BLOCKING (Must Address)

**B1: Examples directory path mismatch in README.md:**
- Description: README.md references `plugin/examples/` multiple times, but examples are at `examples/` (root level), not `plugin/examples/`
- Location: README.md lines 1346, 1380-1386
- Impact: Users following documentation will not find example files where indicated
- Action: Change `plugin/examples/` to `examples/` in all references

**B2: Context examples directory does not exist:**
- Description: CONVENTIONS.md line 368-373 references `plugin/core/examples/context/` for working examples, but this directory does not exist
- Location: CONVENTIONS.md lines 368-373
- Impact: Users cannot find example context files as documented
- Action: Either create `plugin/core/examples/context/` directory or update path to `examples/context/` (which exists at root level)

**B3: SETUP.md references non-existent plugin/core/gates.json path:**
- Description: SETUP.md lines 60-61, 74, 83-84, 320-335, 634-639 references `${CLAUDE_PLUGIN_ROOT}/core/gates.json` but the actual gates.json is at `plugin/gates.json` (not in core subdirectory)
- Location: SETUP.md multiple locations
- Impact: Users following copy commands will get "file not found" errors
- Action: Update paths to use `${CLAUDE_PLUGIN_ROOT}/gates.json` (without core/)

**B4: SETUP.md references non-existent examples paths:**
- Description: SETUP.md lines 74, 83-84, 320-334 reference `${CLAUDE_PLUGIN_ROOT}/core/examples/strict.json`, `permissive.json`, `pipeline.json` but examples are at root level `examples/` not under `plugin/core/`
- Location: SETUP.md lines 74, 83-84, 320-334
- Impact: Copy commands will fail
- Action: Update to use correct paths (possibly absolute or relative from project root)

**B5: Session state file path discrepancy in README.md:**
- Description: README.md lines 849-851 claims hook session is at `.claude/session/state.json`, but this directory does not exist in the project (verified via ls). The session.ts file references this path but it would only be created on first use.
- Location: README.md lines 849-851
- Impact: MEDIUM - Path is technically correct but may confuse users who look for the file before any hooks have run
- Action: Add clarification that this file is created on first hook execution

**B6: Plugin context directory does not exist:**
- Description: README.md line 143, CONVENTIONS.md line 46, and other locations reference `${CLAUDE_PLUGIN_ROOT}/context/` as a fallback location for context files, but `plugin/context/` directory does not exist
- Location: README.md line 143, CONVENTIONS.md lines 46, 103-107, 119-124
- Impact: Plugin context fallback won't work as documented
- Action: Either create `plugin/context/` directory with default files or remove/clarify this fallback mechanism in docs

---

## SUGGESTIONS (Would Improve Quality)

**S1: HookInput interface documentation is incomplete:**
- Description: TYPESCRIPT.md lines 104-133 documents HookInput interface but is missing some fields that exist in the actual schema (schemas.ts), and some field descriptions may be slightly inaccurate
- Location: TYPESCRIPT.md lines 104-133
- Benefit: Accurate interface documentation prevents developer confusion
- Action: Sync HookInput documentation with actual schemas.ts implementation

**S2: Workflow CLI --pass flag exists but not documented in CLAUDE.md:**
- Description: CLAUDE.md line 107 shows `tsv next --fail` but does not mention the `--pass` flag which exists in the CLI (cli.ts line 129)
- Location: CLAUDE.md lines 104-114
- Benefit: Complete command documentation
- Action: Add `--pass` flag to command documentation

**S3: README.md workflow command syntax differs from actual CLI:**
- Description: README.md lines 575-576 shows `workflow start --task 3.A` and `workflow start --agent xyz123` but CLI uses `tsv` or `turboshovel` commands, not `workflow`
- Location: README.md lines 575-590, 893-997
- Benefit: Consistent command syntax
- Action: Change `workflow` to `tsv` or add note about aliasing

**S4: Missing workflow example file in documented location:**
- Description: README.md line 1346-1350 references `plugin/examples/code-review.workflow.md` but the file exists at `examples/code-review.workflow.md` (root examples dir)
- Location: README.md lines 1346-1350
- Benefit: Users can find example files
- Action: Update path to `examples/code-review.workflow.md`

**S5: Inconsistent naming between "workflow" and "tsv" commands:**
- Description: Documentation mixes `workflow` commands (lines 575-590, 893-997) and `tsv` commands. The actual CLI binary is `tsv`/`turboshovel`, not `workflow`
- Location: README.md throughout workflow section
- Benefit: Reduces user confusion
- Action: Consistently use `tsv` command throughout, with note that `turboshovel` is an alias

**S6: package.json bin field in plugin/core references workflow CLI:**
- Description: plugin/core/package.json has bin entry `"workflow": "dist/cli/workflow-cli.js"` but packages/cli is the actual CLI package. This may cause confusion.
- Location: plugin/core/package.json
- Benefit: Clear separation of concerns
- Action: Consider removing bin entry from core or documenting this is for development only

**S7: CONVENTIONS.md pre/post stage naming inconsistency:**
- Description: CONVENTIONS.md line 168 says stages are `start`/`pre` and `end`/`post` but examples throughout docs use `pre`/`post` for tools and `start`/`end` for commands. Could be clearer about which applies where.
- Location: CONVENTIONS.md lines 165-168
- Benefit: Clearer documentation
- Action: Add explicit guidance on when to use start/end vs pre/post

**S8: README.md Table of Hook Events needs verification:**
- Description: README.md lines 100-122 hook event table should be verified against hooks.json registration. The documented 11 hooks match hooks.json, but some status indicators may need updating.
- Location: README.md lines 100-122
- Benefit: Accurate feature documentation
- Action: Verify "Not implemented" labels are still accurate

---

## Verified Correct

1. **ARCHITECTURE.md exists** - File present at root level
2. **INTEGRATION_TESTS.md exists** - File present at root level
3. **CLI package structure** - packages/cli/src/cli.ts exists with documented commands
4. **CLI binary names** - package.json correctly defines `tsv` and `turboshovel` as bin entries
5. **CLI commands match documentation** - start, next, status, stop, complete, stash, pop, list, gate all exist in cli.ts
6. **--step and --fail flags exist** - Verified in cli.ts lines 127, 129
7. **Workflow state persistence path** - .claude/turboshovel/ structure matches documentation
8. **session.json location** - .claude/turboshovel/session.json exists and matches documentation
9. **workflows/ subdirectory** - .claude/turboshovel/workflows/ exists
10. **gates.json structure** - plugin/gates.json exists with documented gate structure
11. **hooks.json has 11 registered hooks** - Verified all 11 hooks in hooks.json
12. **TypeScript gates location** - plugin/core/src/gates/ exists with plugin-path.ts
13. **Gates index exports** - plugin/core/src/gates/index.ts correctly exports pluginPath
14. **GateResult interface** - Matches documentation in types.ts
15. **npm commands** - plugin/core/package.json has build, test, lint scripts as documented
16. **Example JSON configs exist** - examples/strict.json, permissive.json, pipeline.json all exist
17. **code-review.workflow.md exists** - examples/code-review.workflow.md present
18. **Context examples exist** - examples/context/ has example context files
19. **/turboshovel:verify command exists** - plugin/commands/verify.md present

---

## Assessment

**Conclusion:**
The documentation has several path reference errors where it points to `plugin/core/examples/` or `plugin/examples/` locations that don't exist. The actual example files are at the root `examples/` directory. Similarly, context file fallback locations and gates.json paths need correction. The CLI command implementations are accurate, and the core features work as documented.

**Confidence in findings:**
HIGH - All file existence checks were performed against the actual filesystem. Path discrepancies are definitively verified. The 6 BLOCKING issues represent clear documentation-to-implementation mismatches that would cause user frustration.

**Files verified:**
- /Users/tobyhede/psrc/turboshovel/README.md
- /Users/tobyhede/psrc/turboshovel/CLAUDE.md
- /Users/tobyhede/psrc/turboshovel/SETUP.md
- /Users/tobyhede/psrc/turboshovel/CONVENTIONS.md
- /Users/tobyhede/psrc/turboshovel/TYPESCRIPT.md
- /Users/tobyhede/psrc/turboshovel/packages/cli/README.md
