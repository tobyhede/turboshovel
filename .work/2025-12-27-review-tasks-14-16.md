# Code Review - 2025-12-27

## Status: BLOCKED

<!--
Status guidance:
- BLOCKED: Has BLOCKING issues that must be fixed before merge
- APPROVED WITH (NON-BLOCKING) SUGGESTIONS: Ready to merge, but consider addressing suggestions
- APPROVED: Clean, ready to merge with no issues

Note: Tests and checks are assumed to pass. This review focuses on code quality.
-->

## BLOCKING (Must Fix Before Merge)

**Incomplete test migration - 6 test files not updated:**
- Description: Six test files in `plugin/core/__tests__/workflow/` still import from deleted local workflow modules instead of `@turboshovel/shared`, causing TypeScript compilation failures.
- Location:
  - `plugin/core/__tests__/workflow/parser/parser.test.ts` (imports from `../../../src/workflow/parser/types.js`)
  - `plugin/core/__tests__/workflow/parser/helpers.test.ts` (imports from `../../../src/workflow/parser/helpers.js`)
  - `plugin/core/__tests__/workflow/hooks/task-tracker.test.ts` (imports from `../../../src/workflow/types.js`)
  - `plugin/core/__tests__/workflow/hooks/subagent-start.test.ts` (imports from `../../../src/workflow/types.js`)
  - `plugin/core/__tests__/workflow/hooks/substitute.test.ts` (imports from `../../../src/workflow/task-id.js` and `../../../src/workflow/types.js`)
  - `plugin/core/__tests__/workflow/hooks/subagent-stop.test.ts` (imports from `../../../src/workflow/types.js`)
- Action: Update these 6 test files to import from `@turboshovel/shared` instead of the deleted local modules. Follow the pattern used in the other 11 test files successfully migrated in Task 14 (commit 63cf597 and fe4abdc).

**Jest tsconfig module configuration inconsistency:**
- Description: The `jest.config.cjs` uses `module: 'commonjs'` in the ts-jest tsconfig override (line 19), but the actual `tsconfig.json` uses `module: 'NodeNext'` (line 4). This causes `import.meta` to fail when Jest transpiles source files that use ESM features, specifically `src/gates/plugin-path.ts` which uses `import.meta.url`.
- Location: `plugin/core/jest.config.cjs:19` and `plugin/core/tsconfig.json:4`
- Action: Update `jest.config.cjs` tsconfig override to use `module: 'NodeNext'` to match the project's actual TypeScript configuration, or ensure the Jest configuration properly handles ESM-to-CJS transpilation for `import.meta`.


## NON-BLOCKING (May Be Deferred)

**workspace:* protocol change rationale unclear:**
- Description: The commit message states "Change workspace:* to * for npm compatibility" and mentions "npm 11.x doesn't support workspace: protocol", but npm 11.6.0 (confirmed installed) should support workspace dependencies. The actual issue may be related to how the package is published, not local development.
- Location: `packages/cli/package.json:32`, `plugin/core/package.json:32`
- Action: Document the specific npm version/scenario where `workspace:*` fails. If this change is for npm publish compatibility (workspace: protocol doesn't resolve in published packages), clarify in commit message or add a comment explaining this is for publication compatibility, not a version limitation.

**Missing rationale for Jest configuration strategy:**
- Description: The `jest.config.cjs` uses `moduleNameMapper` to point `@turboshovel/shared` directly at TypeScript source (`<rootDir>/../../packages/shared/src/index.ts`) rather than using the built package. While this works, the rationale isn't documented.
- Location: `plugin/core/jest.config.cjs:11`
- Action: Add comment explaining why tests import from TypeScript source rather than built package (likely: avoid build dependency order, faster test iteration, simpler debugging).

**Test results claim inaccurate:**
- Description: Commit message for fe4abdc claims "255 of 258 tests now passing (3 minor failures to investigate)", but test output shows "12 failed test suites" with 6 test files failing to compile, plus 2 runtime failures in builtin-gates.test.ts. This is more than 3 failures and includes compilation errors, not just test failures.
- Location: Commit fe4abdc message vs actual test output
- Action: Update commit message or documentation to accurately reflect test status: "255/258 tests passing in successfully compiled suites; 6 test files require import migration; builtin-gates.test.ts has 2 failures due to import.meta/Jest config issue."

**Deleted code verification:**
- Description: Task 15 deleted 2,028 lines across 16 files from packages/cli. While this appears correct (migrating to shared package), the review assumes these deletions were verified against what's exported from `@turboshovel/shared`.
- Location: All deleted files in commit 1f12daa
- Action: Explicitly verify all deleted functionality is available from `@turboshovel/shared` by checking the shared package exports match what was deleted.


## Checklist

**Security & Correctness:**
- [x] No security vulnerabilities (SQL injection, XSS, CSRF, exposed secrets)
- [x] No insecure dependencies or deprecated cryptographic functions
- [x] No critical logic bugs (meets acceptance criteria)
- [x] No race conditions, deadlocks, or data races
- [x] No unhandled errors, rejected promises, or panics
- [x] No breaking API or schema changes without migration plan

**Testing:**
- [ ] All tests passing (unit, integration, property-based where applicable) - **BLOCKING: 6 test files fail to compile**
- [x] New logic has corresponding tests - N/A (refactoring only)
- [x] Tests cover edge cases and error conditions
- [x] Tests verify behavior (not implementation details)
- [x] Property-based tests for mathematical/algorithmic code with invariants
- [x] Tests are isolated (independent, don't rely on other tests)
- [x] Test names are clear and use structured arrange-act-assert patterns

**Architecture:**
- [x] Single Responsibility Principle (functions/files have one clear purpose)
- [x] No non-trivial duplication (logic that if changed in one place would need changing elsewhere) - excellent deduplication via shared package
- [x] Clean separation of concerns (business logic separate from data marshalling)
- [x] No leaky abstractions (internal details not exposed)
- [x] No over-engineering (YAGNI - implement only current requirements)
- [x] No tight coupling (excessive dependencies between modules) - actually reduces coupling via shared package
- [x] Proper encapsulation (internal details not exposed across boundaries)
- [x] Modules can be understood and tested in isolation

**Error Handling:**
- [x] No swallowed exceptions or silent failures
- [x] Error messages provide sufficient context for debugging
- [x] Fail-fast on invariants where appropriate

**Code Quality:**
- [x] Simple, not clever (straightforward solutions over complex ones)
- [x] Clear, descriptive naming (variables, functions, classes)
- [x] Type safety maintained
- [x] Follows language idioms and project patterns consistently
- [x] No magic numbers or hardcoded strings (use named constants)
- [x] Consistent approaches when similar functionality exists elsewhere
- [x] Comments explain "why" not "what" (code should be self-documenting)
- [x] Rationale provided for non-obvious design decisions - **NON-BLOCKING: Some configuration decisions lack rationale**
- [x] Doc comments for public APIs

**Process:**
- [x] No obvious performance issues (N+1 queries, inefficient algorithms on hot paths)
- [x] ALL linter warnings addressed by fixing root cause (disable/allow/ignore ONLY when unavoidable)
- [ ] Requirements met exactly (no scope creep) - **BLOCKING: Task 14 incomplete**
- [x] No unnecessary reinvention (appropriate use of existing libraries/patterns)


## Next Steps

1. **Address BLOCKING issues:**
   - Update 6 remaining test files to import from `@turboshovel/shared`
   - Fix Jest tsconfig to properly handle `import.meta` (either use NodeNext module or configure proper ESM-to-CJS transpilation)
   - Verify all tests pass after fixes

2. **Consider NON-BLOCKING suggestions:**
   - Document workspace:* → * rationale (likely for npm publish compatibility)
   - Add comments explaining Jest moduleNameMapper strategy
   - Verify deleted code completeness against shared package exports
   - Update commit messages/docs with accurate test status

3. **Ready to merge when:**
   - All tests passing (258/258)
   - BLOCKING issues resolved
   - Status updated to APPROVED or APPROVED WITH SUGGESTIONS

---

## Review Context

**Commits reviewed:**
- `63cf597` - test(core): update test imports to use @turboshovel/shared (9 files)
- `1f12daa` - refactor(cli): import from @turboshovel/shared (16 files deleted, 2,028 lines removed)
- `fe4abdc` - fix: complete test migration to @turboshovel/shared (11 additional files, jest.config.cjs, package.json workspace protocol)

**Files changed:** 74 files, +752 insertions, -4,141 deletions

**Test status:** 255 passing in working suites, 6 test suites failing compilation, 2 tests failing in builtin-gates.test.ts due to import.meta/Jest config issue

**Commands run:**
```bash
git log --oneline -10
git diff HEAD~5..HEAD --stat
git diff fe4abdc^..fe4abdc -- plugin/core/jest.config.cjs
git diff fe4abdc^..fe4abdc -- plugin/core/package.json packages/cli/package.json
npm test  # from plugin/core
find plugin/core/__tests__ -name "*.test.ts" -exec grep -l "from '../../../src/workflow/" {} \;
```
