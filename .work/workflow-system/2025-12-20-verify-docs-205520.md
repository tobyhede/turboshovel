# Review - 2025-12-20

## Metadata
- **Reviewer:** technical-writer (independent verification agent)
- **Date:** 2025-12-20 20:55:20
- **Subject:** plugin/hooks/README.md (Workflow System section)
- **Ground Truth:** Codebase implementation in plugin/hooks/hooks-app/src/workflow/
- **Context:** Independent review #1 for dual-verification
- **Mode:** Review

## Summary
- **Subject:** Workflow System documentation in README.md (lines 389-1104)
- **Scope:** Verified syntax examples, CLI commands, state management, file paths, and integration details against actual TypeScript implementation

---

## Status: APPROVED WITH SUGGESTIONS

## BLOCKING (Must Address)

None

## SUGGESTIONS (Would Improve Quality)

**1. Missing IF/ELSE implementation details:**
- Description: README documents IF/ELSE conditionals (lines 508-518) but the actual parser implementation doesn't appear to support this syntax. The parser only handles PASS/FAIL conditionals, not IF/ELSE variable-based branching.
- Location: README.md:508-518, example workflows at lines 588-590, 854-856, 909-911
- Benefit: Users attempting to use IF/ELSE syntax will get errors. Either document this as planned/future functionality or clarify how IF/ELSE is actually implemented.
- Action: Verify IF/ELSE implementation exists or mark as future feature. Search parser code for IF/ELSE handling.

**2. BLOCKED condition not in parser:**
- Description: README shows `- BLOCKED: STOP "Agent reported BLOCKED"` (line 571, line 890) as a valid condition type, but parser only recognizes PASS and FAIL conditions.
- Location: README.md:571, 890 (execute.workflow.md examples)
- Benefit: Prevents users from writing invalid workflow syntax
- Action: Either implement BLOCKED condition support or document it as custom variable-setting pattern

**3. State persistence path incomplete:**
- Description: README states workflows persist to `.claude/turboshovel/workflows/{id}.json` (line 604) but doesn't mention session.json location for active workflow tracking.
- Location: README.md:604, 633
- Benefit: Complete documentation helps users debug state issues
- Action: Add explicit mention that active workflow ID is in `.claude/turboshovel/session.json` (already mentioned at line 633, but could be clearer in State Persistence section)

**4. Session tracking path inconsistency:**
- Description: README says session.json is at `.claude/turboshovel/workflows/session.json` (line 633) but implementation shows `.claude/turboshovel/session.json` (state.ts:7). README has extra `/workflows/` directory.
- Location: README.md:633 vs src/workflow/state.ts:7
- Benefit: Correct path helps users locate session file
- Action: Fix README to show correct path: `.claude/turboshovel/session.json`

**5. Nested workflow property not documented:**
- Description: WorkflowState type includes `nested` property for nested workflow support (types.ts:96-99) but this feature is not documented in README.
- Location: README.md (missing), types.ts:96-99
- Benefit: If nested workflows are supported, users should know about them. If not yet implemented, remove from types.
- Action: Either document nested workflows or clarify they're planned for future

**6. Complete command not documented:**
- Description: CLI implements `workflow complete` command (workflow-cli.ts:125-152) with `--status` option, but README doesn't mention this command in CLI Commands section.
- Location: README.md:673-774 (CLI Commands), workflow-cli.ts:125-152
- Benefit: Users need to know all available commands
- Action: Add `workflow complete` to CLI Commands section

**7. Example workflow step 1 format differs from docs:**
- Description: README shows explicit **Prompt:** marker as recommended (lines 469-474), but actual example files (execute.workflow.md, code-review.workflow.md) use implicit prompts (no **Prompt:** marker).
- Location: README.md:469-474, 840, 874 vs examples/*.workflow.md
- Benefit: Consistency between documentation and examples helps learning
- Action: Either update examples to use **Prompt:** marker or note that both formats work (which the parser does support)

**8. STOP message syntax clarification:**
- Description: README shows STOP with quoted messages: `STOP "Tests failed"` (line 499, 527, 551) but parser also accepts STOP with parentheses: `STOP (message)` (helpers.ts:115-118). Both formats work but README only shows quotes.
- Location: README.md:527, helpers.ts:115-118
- Benefit: Clear documentation of supported syntax variations
- Action: Note that both `STOP "message"` and `STOP (message)` formats are supported (backward compatibility)

**9. Actions table missing IF/ELSE:**
- Description: Actions Reference table (lines 522-532) doesn't include IF/ELSE conditionals which are shown in examples.
- Location: README.md:522-532 vs 508-518
- Benefit: Complete reference table
- Action: Add IF/ELSE to Actions table or clarify it's a separate conditional type (not an action)

**10. Step content description incomplete:**
- Description: README doesn't explicitly state that step content between header and code block becomes implicit prompt text. This is implemented (parser.ts:157-159, 199-201) but not clearly documented.
- Location: README.md:477-485 (implicit prompt section), parser.ts:199-201
- Benefit: Users understand how step descriptions become prompts
- Action: Add explicit statement: "If a step has no code block and no **Prompt:** marker, all text content becomes the implicit prompt."

**11. Multiple code blocks error message:**
- Description: Parser provides helpful error message for multiple code blocks (parser.ts:123-126: "Multiple code blocks per step not allowed... Combine commands using && or ;") but README doesn't warn about this limitation.
- Location: README.md:458-462 vs parser.ts:123-126
- Benefit: Prevent common mistakes
- Action: Add note in Code Blocks section: "Only one code block per step allowed. Combine multiple commands with && or ;"

**12. findWorkflowFile search paths:**
- Description: CLI searches for workflow files in multiple locations (workflow-cli.ts:268-289) but README only mentions starting from "relative path" or "absolute path".
- Location: README.md:680-688 vs workflow-cli.ts:268-289
- Benefit: Users understand where workflow files are discovered
- Action: Document search order: (1) relative/absolute path as provided, (2) .claude/workflows/ fallback for basename

**13. Default retry max value:**
- Description: README mentions "default max 3" for RETRY (line 531, 997) and shows it in state example (line 613), but it's good to confirm this matches implementation.
- Location: README.md:531, 613, 997 vs state.ts:45
- Benefit: Verified accuracy
- Action: Confirmed accurate - state.ts:45 sets retryMax: 3 by default

**14. Variable type documentation:**
- Description: README shows variables as TypeScript type (line 639: `Record<string, boolean | number | string>`) but examples only show boolean usage.
- Location: README.md:639-652
- Benefit: Show diverse examples
- Action: Add example showing number or string variable usage

**15. Workflow ID format:**
- Description: README shows ID format example (line 608, 736: `wf-2025-01-15-abc123`) but doesn't document the format pattern.
- Location: README.md:608, state.ts:9-14
- Benefit: Users understand ID structure
- Action: Add note: "ID format: wf-YYYY-MM-DD-{random6}" (generated by state.ts)

**16. SubagentStop hook integration claims:**
- Description: README claims SubagentStop hook "Automatically detects task completion" and "Parses STATUS: OK or STATUS: BLOCKED from agent output" (lines 669-671, 1073-1077) but the workflow state manager code doesn't show this parsing logic.
- Location: README.md:669-671, 1073-1077 vs state.ts
- Benefit: Accurate documentation of what's implemented vs planned
- Action: Verify this integration exists elsewhere (in hook handlers) or mark as planned feature

**17. SessionStart auto-injection claim:**
- Description: README claims "SessionStart hook: Auto-injects active workflow context" (line 1080) but doesn't show where this is implemented.
- Location: README.md:1080-1083
- Benefit: Verify this feature exists
- Action: Check hook implementation code for SessionStart workflow context injection

**18. Step validation details:**
- Description: Parser validates sequential numbering and rejects gaps (parser.ts:224-232) with helpful error messages, but README only mentions "Non-sequential numbering (1, 3, 4) - rejected with error" without the helpful message.
- Location: README.md:443, parser.ts:228-230
- Benefit: Users know what error to expect
- Action: Consider adding example error message: "Steps must be numbered sequentially. Expected step 2, found step 3."

**19. Backward compatibility syntax:**
- Description: Parser supports old syntax (`Pass:`, `Fail:`, `Go to Step N`, `STOP (message)`) per helpers.ts:100-118, but README doesn't mention backward compatibility.
- Location: helpers.ts:100-118
- Benefit: Users migrating from old workflows know their syntax still works
- Action: Add note about backward compatibility with old conditional syntax (optional improvement)

**20. H1 rejection error message:**
- Description: Parser rejects H1 headers that look like step headers with helpful error (parser.ts:85-92) but README doesn't mention this validation.
- Location: README.md:441-444, parser.ts:85-92
- Benefit: Users understand why they get errors
- Action: Add to invalid formats: "H1 headers that look like steps (# 1. Title) - rejected with helpful message to use H2"

---

## Assessment

**Conclusion:**
The README Workflow System documentation is substantially accurate and comprehensive. The CLI commands, step format, actions, and state management details match the implementation. The main gaps are:

1. IF/ELSE conditionals are documented but implementation not verified in parser code
2. BLOCKED condition appears in examples but not in parser
3. Session.json path has incorrect directory structure
4. Missing documentation for `workflow complete` command
5. Some advanced features (nested workflows, hook integration) mentioned but implementation not verified

The documentation quality is high with good examples and clear structure. The suggestions focus on completeness and accuracy for features that may be partially implemented or planned for future releases.

**Confidence in findings:**
HIGH for parser, CLI, and state management verification (directly inspected TypeScript code). MEDIUM for hook integration claims (would need to inspect hook handler code to fully verify).

Areas requiring additional verification:
- IF/ELSE conditional implementation (may be in hook handlers, not parser)
- BLOCKED condition handling (may be custom pattern, not built-in)
- SubagentStop task detection (likely in hook implementation, not state manager)
- SessionStart workflow context injection (likely in hook implementation)
